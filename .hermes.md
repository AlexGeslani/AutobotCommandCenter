# ACC Dev Hermes operational boundaries

- ACC implementation, testing, builds, commits, and dashboard updates must never pause, disable, remove, force-run, reschedule, or edit the hourly benchmark recap / ACC synchronization job.
- ACC work must never stop or restart active benchmark controllers or model services.
- Coordinate concurrent ACC writes by rereading immediately before mutation, preserving unrelated changes, retrying a conflicted patch once, or deferring the ACC write to the next scheduled benchmark tick.
- The benchmark recap scheduler and active benchmark processes take priority over ACC convenience.
- Do not commit, push, deploy, publish, or change Production without Alphatrion's explicit authorization.
